# messaging architecture using Docker. Up 3 container (Producer → RabbitMQ → Consumer)
# Producer -> port 5001
# Consumer -> listen to queue
docker-compose up --build

curl -X POST http://localhost:5000/send \
-H "Content-Type: application/json" \
-d '{"message":"Hello Lab 5"}'

# Producer: a service that sends messages to a queue.
# RabbitMQ: a message broker that holds messages until they are processed.
# Consumer: a service that receives messages from the queue and performs actions.